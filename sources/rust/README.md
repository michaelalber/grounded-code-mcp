# rust

Rust language and ecosystem references for the `grounded_rust` collection.

Place resources here covering the Rust language, toolchain, and ecosystem. Good candidates include:

- Language fundamentals (The Rust Programming Language / "The Book")
- Memory management, ownership, borrowing, and lifetimes
- Traits, generics, and the type system
- Error handling (`Result`, `Option`, `?` operator, `thiserror`, `anyhow`)
- Async Rust and runtimes (Tokio, async-std)
- Concurrency primitives (`Arc`, `Mutex`, channels, Rayon)
- Unsafe Rust and the Rustonomicon
- Cargo, workspaces, feature flags, and build scripts
- The Rust Standard Library reference
- Rust by Example
- Web frameworks (Axum, Actix-web)
- CLI development (`clap`, `indicatif`)
- Testing patterns (`#[test]`, `proptest`, integration tests)
